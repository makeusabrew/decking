#!/usr/bin/env node
// Generated by CoffeeScript 1.6.3
var Decking, cmd, decking;

Decking = require("../index.js");

options = {
  command: process.argv[2],
  source: process.argv[3]
};

decking = new Decking(options);

decking.execute();
