#!/usr/bin/env coffee
Decking = require "../index.coffee"

cmd = process.argv[2]

throw "Arg required" if not cmd

decking = new Decking process.argv

switch cmd
  when "build" then decking.build()
  when "status" then decking.status()
